echo "================ FILE CONTENT ================"

find . -type f ! -path "./.git/*" ! -name "$OUTPUT" | while read file; do
    echo
    echo "########## $file ##########"
    cat "$file"
done
) > "$OUTPUT"

# podział na 4 równe części
