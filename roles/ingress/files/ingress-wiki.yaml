apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: wiki-lab
spec:
  ingressClassName: nginx
  rules:
  - host: wiki.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: wiki-service
            port:
              number: 80
